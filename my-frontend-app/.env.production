# Production environment — served from the same Express backend,
# so all API calls use relative paths (e.g. /api/vulnerabilities).
# Leave VITE_API_URL empty so fetch() defaults to the current origin.
VITE_API_URL=
